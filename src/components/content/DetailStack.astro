---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
const classes = ["detail-stack", className ?? ""].filter(Boolean).join(" ");
---

<div class={classes}>
  <slot />
</div>

