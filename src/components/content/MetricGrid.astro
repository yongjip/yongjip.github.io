---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
const classes = ["metric-grid", className ?? ""].filter(Boolean).join(" ");
---

<div class={classes}>
  <slot />
</div>

