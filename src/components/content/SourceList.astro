---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
const classes = ["source-list", className ?? ""].filter(Boolean).join(" ");
---

<div class={classes}>
  <slot />
</div>

