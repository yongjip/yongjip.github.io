---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
const classes = ["detail-columns", className ?? ""].filter(Boolean).join(" ");
---

<div class={classes}>
  <slot />
</div>

