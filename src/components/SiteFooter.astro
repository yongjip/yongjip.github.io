---
interface Props {
  text: string;
  contactLabel?: string;
  contactHref?: string;
}

const { text, contactLabel, contactHref } = Astro.props;
---

<footer class="site-footer">
  <p>{text}</p>
  {contactLabel && contactHref && <a href={contactHref}>{contactLabel}</a>}
</footer>
